# O método intersection retorna um novo conjunto contendo os elementos que estão presentes em ambos os conjuntos.
conjunto_a = {1, 2, 3}
conjunto_b = {3, 4, 5}

conjunto_a.intersection(conjunto_b) # {3}